amends "template.pkl"

import "label.pkl"

services {
  default {
    metadata {
      namespace = "khand-dev"
      labels {
        ...label.matchModule(module)
      }
    }
    spec {
      selector = label.matchModule(module)
    }
  }
}
