amends "template.pkl"

services {
  default {
    metadata {
      namespace = "khand-dev"
      labels {
        ["app"] = module.app
        ["env"] = module.env
      }
    }
    spec {
      selector {
        ["app"] = module.app
        ["env"] = module.env
      }
    }
  }
}
