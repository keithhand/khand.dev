amends "template.pkl"

import "label.pkl"

ingresses {
  default {
    metadata {
      namespace = "khand-dev"
      labels {
        ...label.matchModule(module)
      }
      annotations {
        ["cert-manager.io/cluster-issuer"] = "khand-dev"
      }
    }
  }
}
